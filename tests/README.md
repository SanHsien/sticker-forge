# tests

放自動化測試。

優先測：

- prompt template CLI 輸出。
- prompt template 渲染。
- 3x3 grid 切圖。
- chroma-key 去背。
- LINE ZIP 結構。
- LINE ZIP validator。
- PNG-only stickers ZIP。
- main / tab image 生成。
- padding。

執行：

```powershell
python -m pytest
```
