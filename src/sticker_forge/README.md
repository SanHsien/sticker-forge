# sticker_forge

本機工具核心模組位置。

目前模組：

- `spec`：LINE 尺寸、張數、chroma-key 與調校 profile。
- `prompts`：提示詞模板渲染。
- `splitter`：3x3 grid 切圖。
- `cleanup`：chroma-key 去背。
- `exporter`：LINE 靜態貼圖 ZIP 匯出與驗證。
- `cli`：最小 CLI 入口。

後續模組：

- `app`：GUI 入口。
